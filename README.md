# 🧠 Modbus Connected Home

**Modbus Connected Home** — это пользовательское дополнение для [Home Assistant](https://www.home-assistant.io/), которое позволяет интегрировать устройства по протоколу **Modbus RTU или TCP** через пользовательский Python-скрипт.

---

## ⚙️ Возможности

- Поддержка устройств Modbus RTU (через /dev/ttyUSBx) и Modbus TCP  
- Автоматическое определение устройства из файла `poller.yaml`  
- Простая настройка через `/config/modbus/poller.yaml`  
- Логирование всех событий в стандартный журнал Home Assistant  
- Работает на всех архитектурах: `aarch64`, `amd64`, `armhf`, `armv7`, `i386`  

---

## 🗂️ Структура конфигурации

Файл конфигурации `poller.yaml` должен находиться по пути:


